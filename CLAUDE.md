# CLAUDE.md

## 커밋 메시지 컨벤션

Conventional Commits 형식을 따른다: `<type>: <설명>`

- `feat`: 새 기능 추가
- `fix`: 버그 수정
- `docs`: 문서만 변경 (README, DB_SCHEMA.md, TODO.md 등)
- `refactor`: 동작은 그대로, 코드 구조만 개선
- `chore`: 빌드/설정/의존성 등 잡무성 변경
- `test`: 테스트 추가/수정
- `style`: 포맷팅 등 코드 의미에 영향 없는 변경

여러 성격이 섞인 작업은 한 커밋에 몰아넣지 말고 성격별로 나눠서 커밋한다.

## 커밋 시점

파일 하나 고칠 때마다 바로 커밋/push하지 않는다. 관련된 변경들을 모아뒀다가, 커밋하기 적절한 시점이라고 판단되면 **먼저 사용자에게 물어보고** 진행한다 (어떤 파일을 어떤 메시지로 묶어서 커밋할지 제안한 뒤 승인받기). push도 마찬가지로 매번 확인 후 진행한다.

단, 사용자가 이미 특정 워크플로우 전체를 명시적으로 승인한 경우(예: `shared-docs-editor` 서브에이전트의 dev 브랜치 커밋/push/merge 흐름)는 예외로, 그 안에서는 매 단계 재확인 없이 진행한다.
